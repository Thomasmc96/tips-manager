import React, { useState } from "react";

const TipsModal = ({ data }) => {
  const [isOpen, setIsOpen] = useState(false);

  const toggleModal = () => {
    setIsOpen(!isOpen);
  };

  return (
   <>
                
    </>
  );
};

export default TipsModal;
